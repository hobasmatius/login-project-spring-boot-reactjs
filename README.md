This is an app built with ReactJS and Java Spring Boot.

Run ```mvn clean install``` to fetch all dependencies.

To access the app, simply open ```localhost:8080``` with login details as below:

```username```: user1

```password```: thisishobas1234
